# Node.js
node_modules/
npm-debug.log*
yarn-error.log*
.pnpm-debug.log*

# Временные файлы
*.tmp
*.swp
*.swo
*~
.DS_Store

# Бэкапы (лично хозяина)
backups/

# Persist-данные графа (не коммитим)
data/persist/*.json

# Логи (не коммитим)
logs/**/*.log

# Персональные копии (правило 42)
**/[А-Яа-яЁё]*.mjs
**/*(копия)*
**/*(copy)*
**/*=*
**/*.bak
**/*.old

# Python
__pycache__/
*.py[cod]
venv/
.pytest_cache/

# Прочее
.cache/
dist/
build/

# ==== ЗАЩИТА ОТ ПРИВАТНОГО (обновлено 19.09.2026) ====
# ВАЖНО: data/ теперь ПУБЛИЧНАЯ, её НЕ игнорируем!

# Приватные папки
logs/
backups/
reports/
snapshots/
snapshots_full/
runs/
PROJECT/
AI_MEMORY/
node_modules/

# Но внутри data/ эти подпапки — служебные:
data/feeds/*.backup*
data/feeds/*.opml.backup*
data/**/*.zip
data/**/*.tar.gz

# Архивы и бэкапы (везде)
*.tar.gz
*.zip
*.7z
*.bak
*.bak2
*.bak_*
*.bak.*
*.backup
*.backup_*
*.backup-*
*.old
*.orig
*.tmp
*.swp
*.swo
*~
*_backup_*
*копия*
*(копия)*

# Логи и секреты
*.log
.env
.env.local
.env.*.local
!.env.example

# Черновики
новый_файл*
новый файл*
как использовать*
Чат.txt
