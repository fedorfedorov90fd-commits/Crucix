# lenses-api

## 📋 О МОДУЛЕ / ABOUT

**Русский:**
Тематические линзы — профили интерфейса по темам.

**English:**
Thematic lenses — interface profiles by topic.

---

## 🎯 НАЗНАЧЕНИЕ / PURPOSE

**Русский:**
Модуль `lenses-api` относится к категории **Интерфейс / UI**.

**English:**
Module `lenses-api` belongs to the **Интерфейс / UI** category.

---

## 🚀 КАК ИСПОЛЬЗОВАТЬ / HOW TO USE

**Русский:**
1. Модуль доступен через API: `/api/lenses-api/`
2. Для проверки статуса: `GET /api/lenses-api/status`
3. Для получения данных: `GET /api/lenses-api/`

**English:**
1. Module is available via API: `/api/lenses-api/`
2. For status check: `GET /api/lenses-api/status`
3. For data retrieval: `GET /api/lenses-api/`

---

## 📍 МЕСТОПОЛОЖЕНИЕ / LOCATION

| Файл / File | Путь / Path |
|-------------|-------------|
| API | `apis/sources/lenses-api.mjs` |
| Страница / Page | /lenses-api |
| Справка / Help | `docs/help/lenses-api.md` |

---

## 📊 СТАТУС / STATUS

**Русский:**
🟡 Неактивен (есть в папке) / Inactive (in folder)

**English:**
🟡 Inactive (есть в папке) / Inactive (in folder)

---

## 🔗 СВЯЗАННЫЕ МОДУЛИ / RELATED MODULES

- [Связанные модули будут добавлены позже]

---

**Версия:** 1.0
**Дата обновления:** 2026-08-17
**Категория:** Интерфейс / UI
