# router

## 📋 О МОДУЛЕ / ABOUT

**Русский:**
Адаптивный шлюз для маршрутизации запросов между модулями.

**English:**
Adaptive gateway for routing requests between modules.

---

## 🎯 НАЗНАЧЕНИЕ / PURPOSE

**Русский:**
Модуль `router` относится к категории **Ядро / Core**.

**English:**
Module `router` belongs to the **Ядро / Core** category.

---

## 🚀 КАК ИСПОЛЬЗОВАТЬ / HOW TO USE

**Русский:**
1. Модуль доступен через API: `/api/router/`
2. Для проверки статуса: `GET /api/router/status`
3. Для получения данных: `GET /api/router/`

**English:**
1. Module is available via API: `/api/router/`
2. For status check: `GET /api/router/status`
3. For data retrieval: `GET /api/router/`

---

## 📍 МЕСТОПОЛОЖЕНИЕ / LOCATION

| Файл / File | Путь / Path |
|-------------|-------------|
| API | `apis/sources/router.mjs` |
| Страница / Page | Нет страницы / No page (только API / API only) |
| Справка / Help | `docs/help/router.md` |

---

## 📊 СТАТУС / STATUS

**Русский:**
🟢 Активен (ядро) / Active (core)

**English:**
🟢 Active (ядро) / Active (core)

---

## 🔗 СВЯЗАННЫЕ МОДУЛИ / RELATED MODULES

- [Связанные модули будут добавлены позже]

---

**Версия:** 1.0
**Дата обновления:** 2026-08-17
**Категория:** Ядро / Core
